module.exports = {
    create: function (configuration) {
        var provider = (configuration && configuration.data && configuration.data.provider) || 'sql';
        return require('azure-mobile-apps.data.' + provider)(configuration);
    }
}
