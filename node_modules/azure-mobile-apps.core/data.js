module.exports = {
    create: function (configuration) {
        var platform = (configuration && configuration.data && configuration.data.platform) || 'sql';
        return require('azure-mobile-apps.data.' + platform)(configuration);
    }
}
