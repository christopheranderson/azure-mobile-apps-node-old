var insert = require('./insert'),
    update = require('./update'),
    del = require('./delete'),
    formatQuery = require('../query/format');

module.exports = {
    insert: insert,
    update: update,
    delete: del,
    read: formatQuery
};
