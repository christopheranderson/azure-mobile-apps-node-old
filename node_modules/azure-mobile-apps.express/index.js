module.exports = function (configuration) {
    configuration = configuration || {};

    return {
        tables: require('./tables')(configuration)
    };
};

module.exports.table = require('./tableConfigurationFactory');