// each table has a few unique configuration properties, attach them to the req object here
module.exports = function (options) {
    return function (req, res, next) {
        req._table = {
            options: options
        }
    }
}