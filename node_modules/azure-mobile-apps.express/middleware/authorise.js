module.exports = function (table) {
    return function (req, res, next) {
        if(table.authorise && !req.azureMobile.user)
            res.status(401).send('You must be logged in to use this application');
        else
            next();
    };
};
