var appFactory = require('azure-mobile-apps.express'),
    expect = require('chai').expect,

    app;

describe('azure-mobile-apps.express.integration.configuration', function () {
    beforeEach(function () {
        app = appFactory({ basePath: __dirname });
        app.tables.import('../configuration/tables');
    });

    //it('loads configuration', function () {
    //    expect(app.tables.configuration.raw).to.not.be.undefined;
    //});

    //it('configures tables router', function () {

    //});

    //it('executes configured tables middleware', function () {

    //});
});
