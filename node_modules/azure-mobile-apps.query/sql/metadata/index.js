var acquire = require('acquire');

module.exports = function (configuration) {
    var tables = {},
        api = function (table) {
            if(tables[table])
                return tables[table];
            return api.refresh(table);
        }

    api.refresh = function (table) {

    }

    return api;
}
