var express = require('express'),
    apiFactory = require('zumo.api');

module.exports = function (configuration) {
    var router = express.Router(),
        api = apiFactory(configuration);
    
    router.route('/api/:apiName/');
    
    
    api.middleware = router;
    
    return api;
};
