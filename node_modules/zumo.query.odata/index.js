var queryjs = require('queryjs'),
    query = require('zumo.query');

var odata = module.exports = {
    parse: function (table, querystring) {
        // parse querystring into parts and pass to queryjs.FromOData
        return new queryjs.Query(table);
    }
};