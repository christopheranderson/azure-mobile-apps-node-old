var promises = require('zumo.core/promises');

module.exports = function (tableName) {
    return {
        // table CRUD functions - refactor out to separate module(s)
        read: function (query) {
            return promises.resolved(query);
        },
        readSingle: function (id) {
            // probably remove this method and pass a query where({ id: xxx }) to read function instead
            return promises.resolved(id);
        },
        create: function (entity) {

        },
        update: function (entity) {

        },
        delete: function (id) {

        }
    }
}